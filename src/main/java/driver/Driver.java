package driver;

public class Driver {

    private ConfigurationManager configurationManager;
    private ImageManager imageManager;

    public Driver(ConfigurationManager manager, ImageManager aggregator) {
        configurationManager = manager;
        imageManager = aggregator;


    }

}
